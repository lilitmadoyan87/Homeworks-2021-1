// Chi stacvel (((

function mixStringChars(str) {
  let arr = str.split('');
  for(let i = 0; i < arr.length; i+=3) {
    arr.splice(arr[i+3], 0, arr[i]).splice(arr[i], 1)
   }
  return arr.toString();
}

let str = 'abcdef';
console.log(mixStringChars(str));
